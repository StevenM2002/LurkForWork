export const comments = ({ userId, userEmail, userName, comment }) => {
    // Create elems
    const div = document.createElement('div');
    const name = document.createElement('h1');
    const email = document.createElement('b');

    // Add attr

    // Add evnt handlers

    // Connect elems
};